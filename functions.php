<?php

////////////////////////////////////////////////////////////////////////

// below code is to register menus with validations

if ( ! function_exists( 'mytheme_register_nav_menu' ) ) {

	function mytheme_register_nav_menu(){
		register_nav_menus( array(
	    	'primary-menu' => __( 'Primary Menu', 'text_domain' ),
	    	'footer_menu'  => __( 'Footer Menu', 'text_domain' ),
		) );
	}
	add_action( 'after_setup_theme', 'mytheme_register_nav_menu', 0 );
}

////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

// below code is to register menus without validations

// register_nav_menus(
//     array('primary-menu' => 'Top Menu')
// );

////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

// it is custom to add class on li in menus

// function add_li_class_onNav($classes, $item, $args) {
//     if(isset($args->add_li_class)){
//         $classes[] = $args->add_li_class;
//     }

//     return $classes;
// }

// add_filter('nav_menu_css_classes', 'add_li_class_onNav', 1, 3);

////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

// To add class on menu li

function add_classes_on_li($classes, $item, $args) {
    $classes[] = 'nav-item';
    return $classes;
  }
add_filter('nav_menu_css_class','add_classes_on_li',1,3);

////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

// to add class on anchor tag inside li

function wpse156165_menu_add_class( $atts, $item, $args ) {
    $class = 'nav-link'; // or something based on $item
    $atts['class'] = $class;
    return $atts;
}
add_filter( 'nav_menu_link_attributes', 'wpse156165_menu_add_class', 10, 3 );

////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

//to add custome header(logo) ootion in admin
add_theme_support('post-thumbnails');


////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

//to add custome header(logo) ootion in admin
add_theme_support('custom-header');


////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////

//To register sidebar
register_sidebar(
	array(
		'name' => 'Sidebar',
		'id' => 'sidebar'
	)
	);


////////////////////////////////////////////////////////////////////////


?>